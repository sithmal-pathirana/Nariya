apiVersion: v1
kind: Service
metadata:
  name: nariya-backend-service
  namespace: default
spec:
  selector:
    app: nariya-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
